mutation setPassword($payload: SetPasswordPayload!) {
  setPassword(payload: $payload) {
    user {
      id
      displayName
      email
      username
      changePasswordAtNextLogin
    }
    errors
  }
}
