mutation updateAccountSettings($payload: UpdateAccountSettingsPayload!) {
  updateAccountSettings(payload: $payload) {
    user {
      id
      displayName
      email
      username
      changePasswordAtNextLogin
    }
    errors
  }
}
