mutation changeEmail($payload: ChangeEmailPayload!) {
  changeEmail(payload: $payload) {
    user {
      id
      displayName
      email
      username
      changePasswordAtNextLogin
    }
    errors
  }
}
