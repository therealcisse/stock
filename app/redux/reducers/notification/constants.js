import keyOf from 'keyOf';
// ------------------------------------
// Constants
// ------------------------------------
export const REMOVE_NOTIFICATION = keyOf({ REMOVE_NOTIFICATION: null });
export const UPDATE_NOTIFICATION = keyOf({ UPDATE_NOTIFICATION: null });
